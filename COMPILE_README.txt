SteamPlaylogViewer compile kit

- Uses your provided HTML (Assets/index.html) and embeds it into the EXE.
- The runtime does NOT require Assets/index.html next to the EXE.

Test (no publish):
  build_and_run.cmd

Publish single EXE:
  publish_single_exe.cmd

Output:
  publish\SteamPlaylogViewer.exe
